# Reviewed FP64 output snapshots for `parity_probe`

The probe verifies the reusable uniform-MAC GPU kernels against both their CPU
twins and these immutable snapshots. It intentionally has no automatic blessing
mode: numerical changes must be reviewed before these files are replaced.
