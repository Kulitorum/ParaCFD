; Pin Qt's plugin search root to the application directory so the deployed plugin subfolders
; ({app}\platforms, {app}\styles, {app}\imageformats, ...) are found when launched from anywhere.
[Paths]
Plugins=.
