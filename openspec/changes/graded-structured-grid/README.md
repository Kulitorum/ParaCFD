# graded-structured-grid

Replace the uniform-h MAC grid with an axis-separable graded (stretched) structured grid so a fine core resolves building corners while the far field stays coarse — decoupling feature resolution from domain size.
